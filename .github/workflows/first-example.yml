name: My first Workflow

on: push 

jobs:
    first_jobs:
        runs-on: ubuntu-latest
        steps: 
        - name: Welcome Message 
          run: echo "My first Github Actions Job"
        - name: List files
          run: ls
        - name: Read file
          run: cat README.md